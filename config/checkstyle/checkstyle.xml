<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Style rules for the library backend.

  Deliberately narrower than sun_checks.xml: a ruleset that reports thousands of findings on an
  existing codebase gets switched off within a week. These are the checks that catch real problems
  or real inconsistency, and the build is configured to fail on them - a warning nobody has to fix
  is a warning nobody reads.
-->
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- A file without a newline at the end is a diff that touches two lines instead of one. -->
    <module name="NewlineAtEndOfFile"/>

    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <module name="LineLength">
        <property name="max" value="120"/>
        <!-- URLs in Javadoc and long package/import lines cannot usefully be wrapped. -->
        <property name="ignorePattern" value="^package.*|^import.*|https?://|@see|\{@link"/>
    </module>

    <module name="TreeWalker">

        <!-- Imports -->
        <module name="UnusedImports">
            <property name="processJavadoc" value="true"/>
        </module>
        <module name="RedundantImport"/>
        <module name="AvoidStarImport"/>

        <!-- Naming -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="LocalVariableName"/>
        <module name="ParameterName"/>
        <module name="MemberName"/>
        <module name="ConstantName"/>

        <!-- Braces: the single most common source of a bug that reads correctly. -->
        <module name="NeedBraces"/>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>

        <!-- Whitespace -->
        <module name="WhitespaceAfter"/>
        <module name="NoWhitespaceBefore"/>
        <module name="GenericWhitespace"/>
        <module name="MethodParamPad"/>

        <!-- Real problems rather than style -->
        <module name="EmptyStatement"/>
        <module name="EqualsHashCode"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="StringLiteralEquality"/>
        <module name="MissingSwitchDefault"/>
        <module name="FallThrough"/>
        <module name="IllegalCatch">
            <!-- Catching Throwable or Error swallows OutOfMemoryError. Exception is allowed:
                 the outbound adapters catch it deliberately, to degrade instead of failing. -->
            <property name="illegalClassNames" value="java.lang.Throwable, java.lang.Error"/>
        </module>
        <module name="OneStatementPerLine"/>
        <module name="UpperEll"/>
        <module name="ArrayTypeStyle"/>
        <module name="DefaultComesLast"/>

        <!-- Modifiers -->
        <module name="ModifierOrder"/>
        <module name="RedundantModifier"/>
    </module>
</module>
